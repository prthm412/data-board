export default function HomePage() {
  return (
    <div className="container">
      <div className="card">
        <h1>Welcome</h1>
        <p>Upload CSV datasets, preview them, compute stats, and plot columns against each other.</p>
        <p>Use the nav bar above to get to the Data screen and upload your first dataset.</p>
      </div>
    </div>
  );
}